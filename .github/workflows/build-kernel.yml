name: Build RushKernel

on:
  push:
    branches: [ main ]
    paths:
      - 'config/**'
      - 'anykernel/**'
      - '.github/workflows/build-kernel.yml'
  workflow_dispatch:
    inputs:
      kernel_branch:
        description: 'Kernel source branch (leave empty for default)'
        required: false
        default: ''
      create_release:
        description: 'Create GitHub Release'
        type: boolean
        default: true
      version:
        description: 'Version tag (e.g. v1.0.0)'
        required: false
        default: 'v1.0.0'

env:
  KERNEL_NAME: RushKernel
  DEVICE_CODENAME: b0s
  DEVICE_MODEL: SM-S908B
  KERNEL_REPO: https://github.com/ExtremeXT/android_kernel_samsung_s5e9925
  ANYKERNEL_REPO: https://github.com/osm0sis/AnyKernel3
  ARCH: arm64
  SOC_DEFCONFIG: s5e9925_defconfig
  DEVICE_FRAGMENT: b0s.config
  RUSH_FRAGMENT: rushkernel.config

jobs:
  build:
    name: Build kernel for b0s
    runs-on: ubuntu-22.04
    timeout-minutes: 60

    steps:
      - name: Checkout RushKernel repo
        uses: actions/checkout@v4

      - name: Set up build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison flex libssl-dev make libelf-dev libncurses5-dev zstd lz4 cpio python3 git wget unzip device-tree-compiler ccache gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi binutils-aarch64-linux-gnu binutils-arm-linux-gnueabi

      - name: Set build metadata
        run: |
          echo "BUILD_DATE=$(date +%Y%m%d)" >> $GITHUB_ENV
          echo "SHORT_SHA=$(echo ${GITHUB_SHA} | cut -c1-7)" >> $GITHUB_ENV
          echo "VERSION=${{ inputs.version || 'v1.0.0' }}" >> $GITHUB_ENV
          echo "ZIP_NAME=${{ env.KERNEL_NAME }}-${{ inputs.version || 'v1.0.0' }}-${{ env.DEVICE_CODENAME }}-$(date +%Y%m%d).zip" >> $GITHUB_ENV

      - name: Clone kernel source
        run: |
          if [ -n "${{ inputs.kernel_branch }}" ]; then
            git clone --depth=1 -b "${{ inputs.kernel_branch }}" "${{ env.KERNEL_REPO }}" kernel
          else
            git clone --depth=1 "${{ env.KERNEL_REPO }}" kernel
          fi
          cd kernel
          echo "Cloned commit: $(git rev-parse HEAD)"
          echo "KERNEL_COMMIT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Set up Clang toolchain (auto-detect latest ZyC)
        run: |
          mkdir -p toolchain/clang
          cd toolchain/clang
          LATEST_URL=$(curl -s https://raw.githubusercontent.com/ZyCromerZ/Clang/main/Clang-15-link.txt | tr -d '[:space:]')
          echo "Downloading from: $LATEST_URL"
          wget -q "$LATEST_URL" -O clang.tar.gz
          ls -la clang.tar.gz
          tar -xzf clang.tar.gz
          rm clang.tar.gz
          ls bin/clang
          bin/clang --version

      - name: Apply RushKernel customizations
        run: |
          cp config/${{ env.RUSH_FRAGMENT }} kernel/arch/arm64/configs/${{ env.RUSH_FRAGMENT }}
          echo "RushKernel custom config applied"
          ls -la kernel/arch/arm64/configs/ | grep -E "b0s|rushkernel|s5e9925"

      - name: Configure kernel
        run: |
          cd kernel
          export PATH="${GITHUB_WORKSPACE}/toolchain/clang/bin:${PATH}"
          make O=out ARCH=${{ env.ARCH }} \
            LLVM=1 LLVM_IAS=1 \
            CC=clang \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
            ${{ env.SOC_DEFCONFIG }} \
            ${{ env.DEVICE_FRAGMENT }} \
            ${{ env.RUSH_FRAGMENT }}

      - name: Build kernel
        run: |
          cd kernel
          export PATH="${GITHUB_WORKSPACE}/toolchain/clang/bin:${PATH}"
          export KBUILD_BUILD_USER="hacenbm144"
          export KBUILD_BUILD_HOST="RushKernel-CI"
          make -j$(nproc) O=out ARCH=${{ env.ARCH }} \
            LLVM=1 LLVM_IAS=1 \
            CC=clang \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
            2>&1 | tee ../build.log

      - name: Verify build output
        run: |
          cd kernel/out/arch/arm64/boot
          ls -la
          file Image* dtb* dtbo* 2>/dev/null || true
          [ -f Image ] || [ -f Image.gz ] || [ -f Image.lz4 ] || (echo "ERROR: No kernel image produced" && exit 1)

      - name: Set up AnyKernel3
        run: |
          git clone --depth=1 ${{ env.ANYKERNEL_REPO }} AnyKernel3
          rm -f AnyKernel3/anykernel.sh
          cp anykernel/anykernel.sh AnyKernel3/anykernel.sh
          chmod +x AnyKernel3/anykernel.sh

      - name: Copy kernel image to AnyKernel3
        run: |
          if [ -f kernel/out/arch/arm64/boot/Image.lz4 ]; then
            cp kernel/out/arch/arm64/boot/Image.lz4 AnyKernel3/
          elif [ -f kernel/out/arch/arm64/boot/Image.gz ]; then
            cp kernel/out/arch/arm64/boot/Image.gz AnyKernel3/
          elif [ -f kernel/out/arch/arm64/boot/Image ]; then
            cp kernel/out/arch/arm64/boot/Image AnyKernel3/
          fi
          if [ -f kernel/out/arch/arm64/boot/dtbo.img ]; then
            cp kernel/out/arch/arm64/boot/dtbo.img AnyKernel3/
          fi

      - name: Package AnyKernel3 zip
        run: |
          cd AnyKernel3
          zip -r9 "../${{ env.ZIP_NAME }}" . -x ".git/*" "README.md" "*.gitignore"
          cd ..
          ls -la ${{ env.ZIP_NAME }}
          sha256sum ${{ env.ZIP_NAME }} > ${{ env.ZIP_NAME }}.sha256

      - name: Upload kernel zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}-${{ env.SHORT_SHA }}
          path: |
            ${{ env.ZIP_NAME }}
            ${{ env.ZIP_NAME }}.sha256
          retention-days: 30

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ env.BUILD_DATE }}-${{ env.SHORT_SHA }}
          path: build.log
          retention-days: 14

      - name: Create GitHub Release
        if: github.event_name == 'workflow_dispatch' && inputs.create_release == true
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}-${{ env.BUILD_DATE }}
          name: RushKernel ${{ env.VERSION }} (${{ env.BUILD_DATE }})
          body: |
            # RushKernel ${{ env.VERSION }}

            Custom kernel for **Samsung Galaxy S22 Ultra (Exynos)** — `b0s` / `SM-S908B`

            ## Build info
            - Build date: `${{ env.BUILD_DATE }}`
            - Kernel source: ExtremeXT/android_kernel_samsung_s5e9925 @ `${{ env.KERNEL_COMMIT }}`
            - Toolchain: ZyCromerZ Clang 15

            ## Features
            - **BBR** TCP congestion control (default)
            - **FQ** packet scheduler (default)
            - **TCP Fast Open**
            - **MGLRU** Multi-Gen LRU memory management
            - **ZRAM** with zstd compression
            - **ZSWAP** enabled by default
            - **WireGuard** built-in

            ## Installation
            See INSTALL.md for full instructions.

            ## Disclaimer
            Only flash on **SM-S908B (b0s)**.
          files: |
            ${{ env.ZIP_NAME }}
            ${{ env.ZIP_NAME }}.sha256
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}